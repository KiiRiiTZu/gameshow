{
  "name": "gameshow",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Browserbasierte Multiplayer-Gameshow für einen Moderator und zwei Teams.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "start": "node scripts/dev-server.mjs 8000"
  },
  "engines": {
    "node": ">=20"
  }
}
